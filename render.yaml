services:
  - type: web
    name: vybesync-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --preload --workers 2 app:app
    envVars:
      - key: GENIUS_TOKEN
        sync: false
